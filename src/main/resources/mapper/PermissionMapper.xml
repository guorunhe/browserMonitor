<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shzu.convenience.mapper.PermissionMapper">
    <insert id="addPermission">
        insert into sys_permission(name, url, description) values(#{permission.name}, #{permission.url}, #{permission.description})
    </insert>
    <update id="updatePermissionById">
        update sys_permission set name = #{permission.name} and url = #{permission.url} and description = #{permission.description} where id = #{permission.id}
    </update>
    <select id="findAllPermission" resultType="com.shzu.convenience.entity.Permission">
        select id, name, url, description from sys_permission
    </select>
    <delete id="deletePermission">
        delete from sys_permission where id = #{id}
    </delete>
</mapper>